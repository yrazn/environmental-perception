// Legacy compatibility export; the Worker ignores packaged owner identity.
// Sites supplies the current owner through DATA_APP_OWNER_EMAIL_SHA256 at runtime.
export const dataAppOwnerEmailSha256 = "";
